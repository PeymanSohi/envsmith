# Example environment file for smart-envloader
# This file demonstrates various features

# Basic string values
APP_NAME=MyApp
ENVIRONMENT=development

# Numeric values
PORT=8080
TIMEOUT=30.5
MAX_CONNECTIONS=100

# Boolean values (various formats supported)
DEBUG=true
ENABLE_LOGGING=on
MAINTENANCE_MODE=no

# Lists (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1,example.com
FEATURE_FLAGS=dark_mode,beta_features,analytics

# JSON-like values
DATABASE_CONFIG={"host": "localhost", "port": 5432, "ssl": true}
API_KEYS=["key1", "key2", "key3"]

# Variable expansion
BASE_URL=http://localhost:${PORT}
API_URL=${BASE_URL}/api/v1

# Secrets (will be resolved by secret providers)
DB_PASSWORD=secret://env/DB_PASS
API_SECRET=secret://file/./secrets/api.key

# Comments and blank lines are supported
# This is a comment

# Quoted values with spaces
GREETING="Hello, World!"
MULTILINE_DESCRIPTION="This is a multi-line
description with newlines"

# Escaped characters
ESCAPED_QUOTE="He said \"Hello\""
ESCAPED_NEWLINE="Line 1\nLine 2"
